# Managed by Ansible. Local edits are overwritten on the next run.
# That sentence is the whole point of configuration management - and also the
# reason it loses to immutable images. Ask them why on Wednesday afternoon.
NODE_ENV=production
LOG_LEVEL=info
DATABASE_URL=postgresql://{{ daig_db_user }}:{{ daig_db_password }}@{{ hostvars['db-01'].ansible_host }}:5432/{{ daig_db_name }}
ORDERS_URL=http://orders:3001
KITCHEN_URL=http://kitchen:3002
DISPATCH_URL=http://dispatch:3003
OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4318
